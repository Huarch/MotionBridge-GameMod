diff --git a/packages/f8pyengine/f8pyengine/operators/bone_selector.py b/packages/f8pyengine/f8pyengine/operators/bone_selector.py
index d592fad..99c88c1 100644
--- a/packages/f8pyengine/f8pyengine/operators/bone_selector.py
+++ b/packages/f8pyengine/f8pyengine/operators/bone_selector.py
@@ -12,6 +12,7 @@ from f8pysdk.specs import (
     F8StateAccess,
     F8StateSpec,
     array_schema,
+    boolean_schema,
     complex_object_schema,
     number_schema,
     string_schema,
@@ -24,6 +25,25 @@ from ..constants import SERVICE_CLASS
 
 OPERATOR_CLASS = "f8.bone_selector"
 
+_SELECTION_MODES = ("exact", "exporter_priority")
+_DEFAULT_FUNCTIONAL_BONES = [
+    "Penis01",
+    "Penis02",
+    "Penis09",
+    "R_Hand",
+    "L_Hand",
+    "R_Foot",
+    "L_Foot",
+    "M_Jaw",
+    "M_Jaw_master",
+    "Jaw_master",
+    "M_TongueRoot",
+    "M_Gen",
+    "M_AnusInside",
+    "M_Anus_Inside",
+    "M_Anus_Inside1",
+]
+
 
 def _bone_schema() -> F8ComplexObjectTypeSchema:
     return complex_object_schema(
@@ -65,29 +85,83 @@ class BoneSelectorRuntimeNode(OperatorNode):
         )
         self._initial_state = dict(initial_state or {})
         self._target = str(self._initial_state.get("target", "") or "")
+        self._selection_mode = _selection_mode(self._initial_state.get("selectionMode"))
+        self._enabled_bones = _string_list(self._initial_state.get("enabledBones"))
+        self._allow_unranked_fallback = _boolean_or_default(
+            self._initial_state.get("allowUnrankedFallback"),
+            False,
+        )
         self._available_bones: list[str] = []
         self._available_bones_synced = False
         self._target_synced = False
 
     async def on_state(self, field: str, value: Any, *, ts_ms: int | None = None) -> None:
         del ts_ms
-        if str(field or "").strip() != "target":
-            return
-        self._target = str(value or "").strip()
-        self._target_synced = True
+        name = str(field or "").strip()
+        if name == "target":
+            self._target = str(value or "").strip()
+            self._target_synced = True
+        elif name == "selectionMode":
+            self._selection_mode = _selection_mode(value)
+        elif name == "enabledBones":
+            self._enabled_bones = _string_list(value)
+        elif name == "allowUnrankedFallback":
+            self._allow_unranked_fallback = _boolean_or_default(value, False)
+
+    async def validate_state(
+        self,
+        field: str,
+        value: Any,
+        *,
+        ts_ms: int | None = None,
+        meta: dict[str, Any] | None = None,
+    ) -> Any:
+        del ts_ms, meta
+        name = str(field or "").strip()
+        if name == "selectionMode":
+            normalized = _selection_mode(value)
+            if str(value or "").strip().lower() not in _SELECTION_MODES:
+                raise ValueError("selectionMode must be exact or exporter_priority")
+            return normalized
+        if name == "enabledBones":
+            return _string_list(value)
+        if name == "allowUnrankedFallback":
+            return _boolean_or_default(value, False)
+        if name == "target":
+            return str(value or "").strip()
+        return value
 
     async def compute_output(self, port: str, ctx_id: str | int | None = None) -> Any:
-        if str(port or "").strip() != "bone":
+        port_name = str(port or "").strip()
+        if port_name not in {"bone", "selectedName", "status"}:
             return None
 
         skeleton_value = await self.pull("skeleton", ctx_id=ctx_id)
         bone_names, bones_by_name = self._parse_skeleton(skeleton_value)
         await self._sync_state(bone_names)
 
-        target_name = str(self._target or "").strip()
+        target_name = self._select_target(skeleton_value, bone_names)
         selected = bones_by_name.get(target_name)
         if selected is None:
+            if port_name == "selectedName":
+                return ""
+            if port_name == "status":
+                return {
+                    "valid": False,
+                    "selectedName": "",
+                    "selectionMode": self._selection_mode,
+                    "reason": "no_enabled_ranked_bone",
+                }
             return None
+        if port_name == "selectedName":
+            return target_name
+        if port_name == "status":
+            return {
+                "valid": True,
+                "selectedName": target_name,
+                "selectionMode": self._selection_mode,
+                "reason": "ok",
+            }
         return {
             "name": str(selected["name"]),
             "pos": [float(selected["pos"][0]), float(selected["pos"][1]), float(selected["pos"][2])],
@@ -99,6 +173,19 @@ class BoneSelectorRuntimeNode(OperatorNode):
             ],
         }
 
+    def _select_target(self, skeleton: Any, bone_names: list[str]) -> str:
+        if self._selection_mode == "exact":
+            return str(self._target or "").strip()
+
+        enabled = set(self._enabled_bones)
+        candidates = _preferred_bones(skeleton)
+        if self._allow_unranked_fallback:
+            candidates.extend(name for name in bone_names if name not in candidates)
+        for name in candidates:
+            if name in enabled and name in bone_names:
+                return name
+        return ""
+
     async def _sync_state(self, bone_names: list[str]) -> None:
         if (not self._available_bones_synced) or bone_names != self._available_bones:
             self._available_bones = list(bone_names)
@@ -171,6 +258,49 @@ class BoneSelectorRuntimeNode(OperatorNode):
         }
 
 
+def _trailer(skeleton: Any) -> dict[str, Any]:
+    if not isinstance(skeleton, dict):
+        return {}
+    trailer = skeleton.get("trailer")
+    return trailer if isinstance(trailer, dict) else {}
+
+
+def _preferred_bones(skeleton: Any) -> list[str]:
+    return _string_list(_trailer(skeleton).get("preferredBones"))
+
+
+def _string_list(value: Any) -> list[str]:
+    if not isinstance(value, (list, tuple, set)):
+        return []
+    result: list[str] = []
+    seen: set[str] = set()
+    for item in value:
+        name = str(item or "").strip()
+        if name and name not in seen:
+            seen.add(name)
+            result.append(name)
+    return result
+
+
+def _selection_mode(value: Any) -> str:
+    normalized = str(value or "exact").strip().lower()
+    return normalized if normalized in _SELECTION_MODES else "exact"
+
+
+def _boolean_or_default(value: Any, default: bool) -> bool:
+    if isinstance(value, bool):
+        return value
+    if isinstance(value, (int, float)):
+        return bool(value)
+    if isinstance(value, str):
+        normalized = value.strip().lower()
+        if normalized in {"1", "true", "yes", "on"}:
+            return True
+        if normalized in {"0", "false", "no", "off"}:
+            return False
+    return default
+
+
 BoneSelectorRuntimeNode.SPEC = F8OperatorSpec(
     schemaVersion=F8OperatorSchemaVersion.f8operator_1,
     serviceClass=SERVICE_CLASS,
@@ -194,9 +324,31 @@ BoneSelectorRuntimeNode.SPEC = F8OperatorSpec(
             name="bone",
             description="Selected bone payload `{name,pos,rot}` or None.",
             valueSchema=_bone_schema(),
-        )
+        ),
+        F8DataPortSpec(name="selectedName", description="Currently selected bone name.", valueSchema=string_schema()),
+        F8DataPortSpec(
+            name="status",
+            description="Functional-bone selection status.",
+            valueSchema=complex_object_schema(
+                properties={
+                    "valid": boolean_schema(),
+                    "selectedName": string_schema(),
+                    "selectionMode": string_schema(),
+                    "reason": string_schema(),
+                }
+            ),
+        ),
     ],
     stateFields=[
+        F8StateSpec(
+            name="selectionMode",
+            label="Selection Mode",
+            description="Use one exact bone or the exporter's ranked enabled candidates.",
+            valueSchema=string_schema(default="exact", enum=list(_SELECTION_MODES)),
+            access=F8StateAccess.rw,
+            required=True,
+            showOnNode=True,
+        ),
         F8StateSpec(
             name="target",
             label="Target Bone",
@@ -207,6 +359,26 @@ BoneSelectorRuntimeNode.SPEC = F8OperatorSpec(
             uiControl="select[availableBones]",
             showOnNode=True,
         ),
+        F8StateSpec(
+            name="enabledBones",
+            label="Enabled Detection Bones",
+            description="Enabled functional-bone candidates. Exporter priority chooses the first enabled ranked bone.",
+            valueSchema=array_schema(items=string_schema(), default=list(_DEFAULT_FUNCTIONAL_BONES)),
+            access=F8StateAccess.rw,
+            required=True,
+            uiControl="multiselect[availableBones]",
+            showOnNode=True,
+        ),
+        F8StateSpec(
+            name="allowUnrankedFallback",
+            label="Allow Unranked Fallback",
+            description="Allow unannotated bones after all exporter-ranked candidates.",
+            valueSchema=boolean_schema(default=False),
+            access=F8StateAccess.rw,
+            required=True,
+            uiControl="toggle",
+            showOnNode=False,
+        ),
         F8StateSpec(
             name="availableBones",
             label="Available Bones",
diff --git a/packages/f8pyengine/f8pyengine/operators/skeleton_selector.py b/packages/f8pyengine/f8pyengine/operators/skeleton_selector.py
index 2852d63..89855a2 100644
--- a/packages/f8pyengine/f8pyengine/operators/skeleton_selector.py
+++ b/packages/f8pyengine/f8pyengine/operators/skeleton_selector.py
@@ -23,6 +23,12 @@ from f8pysdk.specs import (
 from ..constants import SERVICE_CLASS
 
 OPERATOR_CLASS = "f8.skeleton_selector"
+_SELECTION_MODES = ("role_index", "exporter_priority")
+_DEFAULT_PARTICIPANT_KEYS = [
+    f"fallen-doll:{role}:{index}"
+    for role in ("male", "female", "other")
+    for index in range(8)
+]
 
 
 class SkeletonSelectorRuntimeNode(OperatorNode):
@@ -34,6 +40,9 @@ class SkeletonSelectorRuntimeNode(OperatorNode):
             state_fields=[state.name for state in (node.stateFields or [])],
         )
         state = dict(initial_state or {})
+        self._selected_key = str(state.get("selectedKey") or "").strip()
+        self._selection_mode = _selection_mode(state.get("selectionMode"))
+        self._enabled_keys = _string_list(state.get("enabledKeys"))
         self._profile_id = str(state.get("profileId") or "").strip()
         self._role = str(state.get("role") or "").strip().lower()
         self._role_index = _integer_or_default(state.get("roleIndex"), 0)
@@ -45,7 +54,13 @@ class SkeletonSelectorRuntimeNode(OperatorNode):
     async def on_state(self, field: str, value: Any, *, ts_ms: int | None = None) -> None:
         del ts_ms
         name = str(field or "").strip()
-        if name == "profileId":
+        if name == "selectedKey":
+            self._selected_key = str(value or "").strip()
+        elif name == "selectionMode":
+            self._selection_mode = _selection_mode(value)
+        elif name == "enabledKeys":
+            self._enabled_keys = _string_list(value)
+        elif name == "profileId":
             self._profile_id = str(value or "").strip()
         elif name == "role":
             self._role = str(value or "").strip().lower()
@@ -73,9 +88,16 @@ class SkeletonSelectorRuntimeNode(OperatorNode):
             return role_index
         if name == "allowLegacyFallback":
             return _boolean_or_default(value, False)
+        if name == "selectionMode":
+            normalized = _selection_mode(value)
+            if str(value or "").strip().lower() not in _SELECTION_MODES:
+                raise ValueError("selectionMode must be role_index or exporter_priority")
+            return normalized
+        if name == "enabledKeys":
+            return _string_list(value)
         if name == "role":
             return str(value or "").strip().lower()
-        if name in {"profileId", "fallbackModelName"}:
+        if name in {"selectedKey", "profileId", "fallbackModelName"}:
             return str(value or "").strip()
         return value
 
@@ -114,6 +136,39 @@ class SkeletonSelectorRuntimeNode(OperatorNode):
         }
 
     def _select(self, candidates: list[dict[str, Any]]) -> dict[str, Any] | None:
+        if self._selected_key:
+            for candidate in candidates:
+                if _stable_key(candidate) == self._selected_key:
+                    return candidate
+            return None
+        if self._selection_mode == "exporter_priority":
+            enabled = set(self._enabled_keys)
+            ranked: list[tuple[int, int, str, dict[str, Any]]] = []
+            for candidate in candidates:
+                if not _has_stable_identity(candidate):
+                    continue
+                key = _stable_key(candidate)
+                if key not in enabled:
+                    continue
+                profile_matches = (
+                    not self._profile_id
+                    or _identity_text(candidate, "profileId").casefold() == self._profile_id.casefold()
+                )
+                role_matches = not self._role or _identity_text(candidate, "role").casefold() == self._role.casefold()
+                if not profile_matches or not role_matches:
+                    continue
+                ranked.append(
+                    (
+                        _identity_integer(candidate, "participantPriority", 1024),
+                        _identity_integer(candidate, "roleIndex", 1024),
+                        key,
+                        candidate,
+                    )
+                )
+            if not ranked:
+                return None
+            ranked.sort(key=lambda item: (item[0], item[1], item[2]))
+            return ranked[0][3]
         for candidate in candidates:
             if not _has_stable_identity(candidate):
                 continue
@@ -212,6 +267,24 @@ def _boolean_or_default(value: object, default: bool) -> bool:
     return default
 
 
+def _selection_mode(value: object) -> str:
+    normalized = str(value or "role_index").strip().lower()
+    return normalized if normalized in _SELECTION_MODES else "role_index"
+
+
+def _string_list(value: object) -> list[str]:
+    if not isinstance(value, (list, tuple, set)):
+        return []
+    result: list[str] = []
+    seen: set[str] = set()
+    for item in value:
+        text = str(item or "").strip()
+        if text and text not in seen:
+            seen.add(text)
+            result.append(text)
+    return result
+
+
 SkeletonSelectorRuntimeNode.SPEC = F8OperatorSpec(
     schemaVersion=F8OperatorSchemaVersion.f8operator_1,
     serviceClass=SERVICE_CLASS,
@@ -241,6 +314,35 @@ SkeletonSelectorRuntimeNode.SPEC = F8OperatorSpec(
         ),
     ],
     stateFields=[
+        F8StateSpec(
+            name="selectedKey",
+            label="Participant",
+            description="Exact live participant. Empty uses the profile/role/index fields below.",
+            valueSchema=string_schema(default=""),
+            access=F8StateAccess.rw,
+            required=True,
+            uiControl="select[availableKeys]",
+            showOnNode=True,
+        ),
+        F8StateSpec(
+            name="selectionMode",
+            label="Participant Selection",
+            description="Use a fixed role index or the first enabled exporter-ranked participant.",
+            valueSchema=string_schema(default="role_index", enum=list(_SELECTION_MODES)),
+            access=F8StateAccess.rw,
+            required=True,
+            showOnNode=True,
+        ),
+        F8StateSpec(
+            name="enabledKeys",
+            label="Enabled Participants",
+            description="Enabled live participant candidates. Clearing all candidates produces no output.",
+            valueSchema=array_schema(items=string_schema(), default=list(_DEFAULT_PARTICIPANT_KEYS)),
+            access=F8StateAccess.rw,
+            required=True,
+            uiControl="multiselect[availableKeys]",
+            showOnNode=True,
+        ),
         F8StateSpec(
             name="profileId",
             label="Profile ID",
diff --git a/packages/f8pyengine/tests/test_bone_selector.py b/packages/f8pyengine/tests/test_bone_selector.py
index 0b423a5..626cb09 100644
--- a/packages/f8pyengine/tests/test_bone_selector.py
+++ b/packages/f8pyengine/tests/test_bone_selector.py
@@ -23,7 +23,11 @@ from f8pyengine.operators.bone_selector import (  # noqa: E402
 )
 
 
-def _skeleton_payload(bones: list[dict[str, Any]]) -> dict[str, Any]:
+def _skeleton_payload(
+    bones: list[dict[str, Any]],
+    *,
+    preferred_bones: list[str] | None = None,
+) -> dict[str, Any]:
     return {
         "type": "skeleton_binary",
         "modelName": "VMC",
@@ -32,7 +36,7 @@ def _skeleton_payload(bones: list[dict[str, Any]]) -> dict[str, Any]:
         "skeletonProtocol": "unity_humanoid",
         "boneCount": len(bones),
         "bones": list(bones),
-        "trailer": None,
+        "trailer": {"preferredBones": list(preferred_bones or [])},
     }
 
 
@@ -127,6 +131,47 @@ class BoneSelectorTests(unittest.IsolatedAsyncioTestCase):
         self.assertTrue(target.found)
         self.assertEqual(str(target.value or ""), "")
 
+    async def test_exporter_priority_falls_back_when_primary_bone_is_disabled(self) -> None:
+        bus, node = await self._build_node(
+            state_values={
+                "selectionMode": "exporter_priority",
+                "enabledBones": ["L_Hand"],
+            }
+        )
+        payload = _skeleton_payload(
+            bones=[
+                {"name": "R_Hand", "pos": [1.0, 0.0, 0.0], "rot": [1.0, 0.0, 0.0, 0.0]},
+                {"name": "L_Hand", "pos": [2.0, 0.0, 0.0], "rot": [1.0, 0.0, 0.0, 0.0]},
+            ],
+            preferred_bones=["R_Hand", "L_Hand"],
+        )
+        buffer_input(bus, "selector1", "skeleton", payload, ts_ms=5, edge=None, ctx_id=5)
+
+        out = await node.compute_output("bone", ctx_id=5)
+        status = await node.compute_output("status", ctx_id=5)
+
+        self.assertEqual(out["name"], "L_Hand")
+        self.assertEqual(status["selectedName"], "L_Hand")
+
+    async def test_exporter_priority_rejects_unannotated_candidates_by_default(self) -> None:
+        bus, node = await self._build_node(
+            state_values={
+                "selectionMode": "exporter_priority",
+                "enabledBones": ["R_Hand", "L_Hand"],
+            }
+        )
+        payload = _skeleton_payload(
+            bones=[
+                {"name": "R_Hand", "pos": [1.0, 0.0, 0.0], "rot": [1.0, 0.0, 0.0, 0.0]},
+                {"name": "L_Hand", "pos": [2.0, 0.0, 0.0], "rot": [1.0, 0.0, 0.0, 0.0]},
+            ]
+        )
+        buffer_input(bus, "selector1", "skeleton", payload, ts_ms=6, edge=None, ctx_id=6)
+
+        self.assertIsNone(await node.compute_output("bone", ctx_id=6))
+        status = await node.compute_output("status", ctx_id=6)
+        self.assertEqual(status["reason"], "no_enabled_ranked_bone")
+
 
 if __name__ == "__main__":
     unittest.main()
diff --git a/packages/f8pyengine/tests/test_skeleton_motion_operators.py b/packages/f8pyengine/tests/test_skeleton_motion_operators.py
index 9429ec9..e2c1820 100644
--- a/packages/f8pyengine/tests/test_skeleton_motion_operators.py
+++ b/packages/f8pyengine/tests/test_skeleton_motion_operators.py
@@ -47,7 +47,14 @@ async def _build_node(runtime_type: type, register: Callable[[Registry], Registr
     return bus, node
 
 
-def _skeleton(*, profile_id: str, role: str, role_index: int, model_name: str) -> dict[str, Any]:
+def _skeleton(
+    *,
+    profile_id: str,
+    role: str,
+    role_index: int,
+    model_name: str,
+    participant_priority: int | None = None,
+) -> dict[str, Any]:
     stable_key = f"{profile_id}:{role.lower()}:{role_index}"
     return {
         "type": "skeleton_binary",
@@ -60,6 +67,7 @@ def _skeleton(*, profile_id: str, role: str, role_index: int, model_name: str) -
             "profileId": profile_id,
             "role": role,
             "roleIndex": role_index,
+            "participantPriority": role_index if participant_priority is None else participant_priority,
             "stableKey": stable_key,
         },
     }
@@ -102,6 +110,66 @@ async def test_skeleton_selector_does_not_guess_when_identity_is_missing() -> No
     assert await node.compute_output("skeleton", ctx_id=1) is None
 
 
+async def test_skeleton_selector_can_choose_exact_live_participant_key() -> None:
+    bus, node = await _build_node(
+        SkeletonSelectorRuntimeNode,
+        register_selector,
+        {"selectedKey": "fallen-doll:male:1", "profileId": "fallen-doll", "role": "male", "roleIndex": 0},
+    )
+    skeletons = [
+        _skeleton(profile_id="fallen-doll", role="male", role_index=0, model_name="MaleA"),
+        _skeleton(profile_id="fallen-doll", role="male", role_index=1, model_name="MaleB"),
+    ]
+    buffer_input(bus, "node1", "skeletons", skeletons, ts_ms=1, edge=None, ctx_id=1)
+
+    selected = await node.compute_output("skeleton", ctx_id=1)
+
+    assert selected["modelName"] == "MaleB"
+
+
+async def test_skeleton_selector_uses_enabled_exporter_participant_priority() -> None:
+    bus, node = await _build_node(
+        SkeletonSelectorRuntimeNode,
+        register_selector,
+        {
+            "selectionMode": "exporter_priority",
+            "enabledKeys": ["fallen-doll:male:1"],
+            "profileId": "fallen-doll",
+            "role": "male",
+        },
+    )
+    skeletons = [
+        _skeleton(
+            profile_id="fallen-doll", role="male", role_index=0, model_name="MaleA", participant_priority=0
+        ),
+        _skeleton(
+            profile_id="fallen-doll", role="male", role_index=1, model_name="MaleB", participant_priority=1
+        ),
+    ]
+    buffer_input(bus, "node1", "skeletons", skeletons, ts_ms=1, edge=None, ctx_id=1)
+
+    selected = await node.compute_output("skeleton", ctx_id=1)
+
+    assert selected["modelName"] == "MaleB"
+
+
+async def test_skeleton_selector_allows_no_enabled_participant() -> None:
+    bus, node = await _build_node(
+        SkeletonSelectorRuntimeNode,
+        register_selector,
+        {
+            "selectionMode": "exporter_priority",
+            "enabledKeys": [],
+            "profileId": "fallen-doll",
+            "role": "male",
+        },
+    )
+    skeletons = [_skeleton(profile_id="fallen-doll", role="male", role_index=0, model_name="MaleA")]
+    buffer_input(bus, "node1", "skeletons", skeletons, ts_ms=1, edge=None, ctx_id=1)
+
+    assert await node.compute_output("skeleton", ctx_id=1) is None
+
+
 async def test_relative_pose_axes_maps_reference_local_y_to_l0() -> None:
     bus, node = await _build_node(RelativePoseAxesRuntimeNode, register_pose_axes, {"primaryAxis": "local_y"})
     reference = {"name": "MalePenisBase", "pos": [1.0, 2.0, 3.0], "rot": [1.0, 0.0, 0.0, 0.0]}
